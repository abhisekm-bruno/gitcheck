info:
  name: Untitled
  type: http
  seq: 2

http:
  method: GET
  url: ww.google.com
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: xsxcaccXcssacxasc

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
